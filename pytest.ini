[pytest]
# 默认门禁只跑 unit + contract；e2e 与集成类需显式指定
testpaths = tests/unit tests/contract
norecursedirs = e2e .git .venv* __pycache__ dist build temp runtime
markers =
    integration: 需要真实外部组件（LibreOffice、IPP 设备、真实 Cloud/PostgreSQL）的测试，默认不随单元门禁运行
addopts = -ra --strict-markers
